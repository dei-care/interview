name: PR

on: pull_request

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build --target lint -t interview-lint -f docker/Dockerfile .
      - run: docker run interview-lint

  coding:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build --target coding -t interview-coding -f docker/Dockerfile .
      - run: docker run interview-coding
